_site
.sass-cache
.DS_Store
*.sw*
.jekyll-metadata

# Ignore the config file that's used for gh-pages.
_config.gh-pages.yml

Gemfile.lock

# Local Strava credentials. Never commit.
.env

# Exploratory notebooks: they store cell output, which has included live
# API credentials in the past.
*.ipynb
.ipynb_checkpoints/

# Superseded by scripts/update_strava_stats.py. Still holds hardcoded
# credentials and disables TLS verification, so keep it out of the repo.
scripts/update_total_stats_0.py
